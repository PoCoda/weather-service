To start application use commands:

    1. mvn clean install
    2. docker build //dot is necessary
    3. docker up

Observe application running on localhost:8887 // /test endpoint to test
and postgres db under url: jdbc:postgresql://weather-db:5432/weather-db

